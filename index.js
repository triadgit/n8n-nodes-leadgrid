// Intentionally empty. n8n discovers the node and credential classes via the
// `n8n.nodes` and `n8n.credentials` arrays in package.json, which point into
// the compiled `dist/` output.
module.exports = {};
